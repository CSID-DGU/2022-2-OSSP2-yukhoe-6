import { CollaborationRole, CollaborationStatus } from "box-node-sdk/lib/schemas/collaboration";
export interface CollaborationUpdate {
    can_view_path?: boolean;
    expires_at?: string;
    role: CollaborationRole;
    status?: CollaborationStatus;
}
